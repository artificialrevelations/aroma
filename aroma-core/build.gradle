apply plugin: 'com.android.library'

android {
    def config = rootProject.extensions.getByName("ext")

    compileSdkVersion config["androidCompileSdkVersion"]
    buildToolsVersion config["androidBuildToolsVersion"]

    defaultConfig {
        minSdkVersion config["androidMinSdkVersion"]
        targetSdkVersion config["androidTargetSdkVersion"]

        versionCode config["versionCode"]
        versionName config["versionName"]

        testApplicationId 'org.artdev.aroma.core.test'
        testInstrumentationRunner 'android.test.InstrumentationTestRunner'
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_6
        targetCompatibility JavaVersion.VERSION_1_6
    }
}

dependencies {
    def aromaCoreDependencies = rootProject.ext.aromaCoreDependencies
    def aromaCoreTestDependencies = rootProject.ext.aromaCoreTestDependencies

    implementation aromaCoreDependencies.androidxAnnotations

    testImplementation aromaCoreTestDependencies.jUnit
    testImplementation aromaCoreTestDependencies.robolectric
    testImplementation aromaCoreTestDependencies.mockitoCore
    testImplementation aromaCoreTestDependencies.dexmaker
    testImplementation aromaCoreTestDependencies.dexmakerMockito
}
